Add your image files here:

  logo-wide.png         — wide logo with name (header on desktop)
  logo-green.png        — square mark (header on mobile, footer, favicon)
  hero.jpg              — optional wide banner on the home page
  about-facility.jpg    — optional photo on the About page
  map-out.png           — overview / driving route to the factory
  map-in.png            — close-up map of the factory location

  Yard-RCP-2.jpg          — hero slides 1 & 2, pipes (values, products)
  products/pipes.jpg      — optional alternate
  curbstone.jpg           — أطاريف / curbstones (home values, feature, products)
  western_interlock_inc_cover.jpg — إنترلوك (home values, feature, products)
  products/interlock.jpg          — optional alternate

Use .jpg or .png; if you use different names, update the src paths in the HTML files.
